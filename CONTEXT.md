# herdr-git-upstream

herdr 사이드바와 worktree 흐름에서 "원격을 알아야만 답할 수 있는 질문"을 대신 답하는 플러그인.
herdr는 원격에 말을 걸지 않으므로, 이 플러그인이 fetch를 맡고 그 결과를 토큰과 화면으로 돌려준다.

## Language

### herdr 쪽

**워크스페이스**:
herdr 사이드바의 한 행. 첫 탭의 뿌리 페인이 보는 셸 작업 디렉터리가 그 정체를 정한다.
_Avoid_: Space, 스페이스

**본 체크아웃**:
저장소의 원래 작업 트리. 연결된 worktree들이 이곳의 참조 저장소를 나눠 쓴다.
_Avoid_: 원본, source checkout, 메인 체크아웃

**worktree**:
같은 저장소에 연결된 별도의 체크아웃. herdr가 만든 것은 워크스페이스 하나에 매인다.
_Avoid_: 워크트리

**들여쓴 행**:
본 체크아웃과 저장소를 공유하는 워크스페이스를 herdr가 자식으로 묶어 그린 사이드바 행.
내장 git 토큰이 지워지는 자리라서 이 플러그인의 토큰이 필요하다.

**토큰**:
워크스페이스에 붙는 이름-값 쌍. 사이드바 `rows`가 `$이름`으로 요청한 것만 그려진다.
_Avoid_: 메타데이터, 배지

### git 쪽

**upstream**:
현재 브랜치가 따라가는 원격 브랜치. `branch.<이름>.remote`와 `branch.<이름>.merge`로 정해진다.
_Avoid_: 상류, 원격 브랜치(뜻이 더 넓다)

**추적 참조**:
upstream을 로컬에 비추어 둔 `refs/remotes/...` 참조. herdr가 앞뒤 수를 셀 때 견주는 대상이며,
fetch가 갱신하는 것이 바로 이것이다.
_Avoid_: 원격 참조, remote ref

**뒤처짐 / 앞섬**:
추적 참조에는 있고 HEAD에는 없는 커밋 수, 그리고 그 반대. `$behind`와 `$ahead`로 보고한다.
_Avoid_: behind/ahead 를 한국어 문장 안에서 그대로 쓰는 것

**stale**:
fetch 실패가 정해진 시간 넘게 이어져 뒤처짐·앞섬 숫자를 믿을 수 없는 상태. 첫 실패로는 되지 않는다.

**깨끗함**:
추적 파일에 변경이 없는 상태. 최신화(빨리 감기)의 조건이며, 추적되지 않은 파일이 있어도 깨끗할 수 있다.
_Avoid_: clean 을 한국어 문장 안에서 그대로 쓰는 것

**손대지 않음**:
추적되지 않은 파일까지 하나도 없는 상태. herdr가 worktree 삭제를 허용하는 조건과 같다.
_Avoid_: 깨끗함(뜻이 좁다), dirty 의 반대말로서의 clean

**기준 브랜치**:
새 worktree가 갈라져 나오는 브랜치. herdr의 기본값은 본 체크아웃의 HEAD다.
_Avoid_: base, 부모 브랜치

**최신화**:
갓 만든 worktree를 기준 브랜치의 추적 참조까지 빨리 감기로만 옮기는 일. 잃을 것이 없을 때만 한다.
_Avoid_: 동기화, 업데이트

**원격 기본 브랜치**:
원격의 HEAD가 가리키는 브랜치. 로컬에는 `refs/remotes/<원격>/HEAD`로 비친다.
_Avoid_: main, master, 기본 브랜치(로컬 기본 브랜치와 혼동됨)

**통합 브랜치**:
작업 브랜치가 병합되어 들어가는 브랜치. 원격 기본 브랜치는 언제나 포함되고, 저장소별로 더 지정할 수 있다.
_Avoid_: 대상 브랜치, target, 병합 대상

**gone**:
upstream 브랜치가 원격에서 사라진 상태. 대개 병합된 뒤 지워진 것이다.
_Avoid_: 삭제됨, missing, pruned

**merged**:
HEAD의 내용이 이미 어느 원격 브랜치에 들어가 있는 상태. 조상 관계이거나, 통합 브랜치에 병합해도
아무것도 바뀌지 않는 경우다. 놓칠 수는 있어도 틀리지는 않는다.
_Avoid_: 병합됨(문장 안에서는 써도 토큰 이름으로는 쓰지 않음), done, finished

**따라잡기**:
뒤처진 브랜치를 추적 참조까지 끌어올리는 일. `$catchup`은 그때 충돌이 나는지를 미리 알려 준다.
_Avoid_: catch-up, pull, 동기화

### 데몬 쪽

**회차**:
데몬이 열린 워크스페이스를 한 바퀴 돌며 fetch하고 토큰을 보고하는 한 번의 일.
_Avoid_: sweep, 사이클, 틱

**쪽지**:
이벤트 훅이 데몬에게 남기는 "지금 한 바퀴 돌라"는 파일. 훅은 네트워크를 타지 않고 이것만 남긴다.
_Avoid_: 시그널, 큐, 메시지

### 화면 쪽

**worktree 화면**:
한 저장소의 worktree 전부를 판정과 함께 표로 보이는 화면. safe인 것만 지울 수 있다.
_Avoid_: 보드, 판, 목록, 대시보드

**생성 팝업**:
기준 브랜치를 골라 새 worktree를 만드는 팝업. herdr 내장 팝업과 달리 기준을 고를 수 있다.
_Avoid_: 생성 화면, 다이얼로그

**판정**:
worktree 화면이 worktree마다 붙이는 네 값 중 하나.
- `blocked`: 본 체크아웃이거나, 잠겼거나, 디렉터리가 사라졌거나, herdr에서 에이전트가 일하는 중. 지울 수 없다.
- `safe`: 손대지 않았고, merged이거나 (gone이면서 앞선 커밋이 0임을 확인할 수 있음). 끝난 일이다.
- `review`: 손댄 것이 있거나, 따라잡을 때 충돌하거나, gone인데 앞선 커밋이 있거나 확인할 수 없음.
- `keep`: 그 밖의 전부. 최신이거나 깨끗하게 따라잡을 수 있는 진행 중인 작업.
_Avoid_: 상태, 등급, 분류
